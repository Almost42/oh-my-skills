# 架构

## 系统形态

- 高层结构概览

## 模块边界

- 主要模块及其职责

## 跨模块依赖

- 关键交互关系与约束

## 扩展点

- 新能力或新模块应如何接入

## 结构约束

- 必须长期保持成立的架构规则

## 构建与验证

- 推荐：写清**唯一或默认**的构建、测试、静态检查、环境验证命令（如 `./mvnw -pl …`、必须附带的 profile；若与「全仓通例」不同，在此显式声明）
- 验收/CI 若与本地命令不同，可注明对应关系
- OMS：Patch 与执行类 skill 会**优先**读本节；内容保持短而可执行，不写任务流水账

## 开发环境与工具约定

- 可选：运行时、包管理器、IDE 或沙箱相关约束（例如仅允许某类 Maven/Gradle 调用、必用 wrapper、需在子目录执行）
- 与结构无关的临时排障可放入 `docs/knowledge/lessons/`，长期规则放此处或 `AGENTS.md`
