# Rationalization 防御

| 常见理由 | 实际含义 |
| :--- | :--- |
| "上次测试过了" | 只有实现指纹匹配的验证快照才能复用；指纹变化必须重新验证 |
| "逻辑上应该没问题" | 逻辑判断不替代运行结果 |
| "只改了很小的地方" | 小改动同样可以引入回归 |
| "构建通过了就行" | 构建通过 ≠ 功能正确 |
| "子代理说成功了" | 子代理报告需要独立验证 |
| "为保险再跑一遍全量" | 没有新增覆盖的重复命令不增加证据，只增加等待 |
